{
  "name": "railway-node-tunnel",
  "version": "1.0.0",
  "private": true,
  "description": "A small HTTP-over-WebSocket tunnel for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "node client.js",
    "check": "node --check server.js && node --check client.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
